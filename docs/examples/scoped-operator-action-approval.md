# Example: Scoped Operator Action Approval

A viewer credential can inspect queue state but cannot set drain mode. A runtime-admin credential can perform the same action, and the request is recorded with scope, request id, and nonce.
