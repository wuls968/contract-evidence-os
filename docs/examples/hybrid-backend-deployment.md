# Hybrid Backend Deployment

A production-shaped deployment uses SQLite for replay and evidence lineage, Redis for fast coordination, and PostgreSQL for durable shared-state records such as quota policies, trust bindings, and reconciliation runs.

